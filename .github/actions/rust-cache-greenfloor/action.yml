---
name: GreenFloor Rust cache
description: Install the stable Rust toolchain and restore GreenFloor Cargo caches.

inputs:
  save-cache:
    description: Save the Cargo cache at job end (disable on lint; test jobs populate it).
    required: false
    default: "true"
  install-nextest:
    description: Install cargo-nextest (disable on jobs that only build or lint).
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable

    - uses: Swatinem/rust-cache@v2
      with:
        # Match the legacy test-job cache key (`v0-rust-test-…`) so lint reuses
        # existing main-branch caches instead of starting a new key namespace.
        shared-key: test
        workspaces: |
          greenfloor-engine -> target
        cache-workspace-crates: true
        save-if: ${{ inputs.save-cache == 'true' }}

    - uses: taiki-e/install-action@v2
      if: ${{ inputs.install-nextest == 'true' }}
      with:
        tool: nextest
