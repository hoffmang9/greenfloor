[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "greenfloor_engine"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "greenfloor_engine"
